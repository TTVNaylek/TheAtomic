"use strict";
const renderLog = (message) => {
    const logBox = document.getElementById("log");
    const logMessage = document.createElement("div").classList.add("log-message");
    //document.getElementById("log").appendChild(logMessage)
};
